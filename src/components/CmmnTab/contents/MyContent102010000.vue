<template>
  <h1>hello</h1>
  <input type="text" />
  <div id="grid"></div>
</template>

<script>
import "./../../../js/grid/aljjabaegi.grid.core-2.0.1"
import "./../../../js/grid/css/aljjabaegi.grid-2.0.0.css"

//gird에 edit버튼이 같이 있기 때문에 commonButton은 추가할 필요 없음

export default {
  name : "MyContent102010000",
  mounted(){
    this.setGrid();
  },
  methods : {
    setGrid() {
      const option = {
      id : 'grid',
      url: '/WEBTEMP/grid/getList.json',  /*조회시 필요한 id*/
      title : 'TI플랫폼팀 MEMBER 목록',
      postData : {
        serviceName : "memberBusiness"
      },
      colN : ['사용자ID', '사용자명', '직급', '부서명', '파트명'], /*표출할 헤더명*/
      colM : [
        {id:'MEMBER_CD'},
          {id:'USER_NAME'},
        {id:'ROLE_NAME'},
        {id:'DEPT_NAME'},
        {id:'PART_NAME'}
      ], /* DB에서 가져올 데이터와 id명을 갖게 맞추어 주어야 매핑이 된다.*/
        onClickRow : function(data, id, index){
          console.log(data, id, index)
        }
      }
      let grid = AG(option);
    }
  }
}
</script>